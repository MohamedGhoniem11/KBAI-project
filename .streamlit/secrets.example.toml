# Streamlit secrets template
# Copy this to .streamlit/secrets.toml (gitignored)
# Or set as environment variables on HuggingFace Spaces / Streamlit Cloud

XAI_API_KEY = "your_xai_api_key_here"
LLM_PROVIDER = "xai"
LLM_MODEL = "grok-3-latest"